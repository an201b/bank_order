RC Patcher (SEPA XML Name Updater)

RC Patcher — это утилита для автоматической актуализации наименований получателей в файлах платежных поручений формата SEPA XML (ISO 20022).

Скрипт сверяет коды предприятий (Juridinių asmenų kodas) из файла платежки с официальной базой данных Центра регистров Литвы (VĮ Registrų centras) и при необходимости заменяет наименование получателя на официально зарегистрированное.

Это позволяет избежать отклонения платежей банком из-за несовпадения имени получателя и его кода, не изменяя при этом данные в учетной системе (например, 1С:Предприятие).
🚀 Возможности

    ⚡ Высокая скорость: Обрабатывает большие базы данных (CSV) в памяти за секунды.

    🔒 Безопасность данных: Работает локально, не передает финансовые данные в интернет.

    🛠 ISO 20022 совместимость: Корректно работает с пространством имен (Namespaces) XML, не нарушая структуру файла.

    🌍 Фильтрация: Игнорирует иностранных получателей (которых нет в реестре Литвы), оставляя их данные без изменений.

    📝 Гибкость: Поддерживает режим создания нового файла или перезаписи исходного.

    🐞 Debug режим: Подробное логирование процесса в консоль (на английском языке).

📋 Требования

    ОС: Windows (при использовании .exe) или любая ОС с поддержкой Python 3.

    Файл базы данных: Актуальный файл открытых данных JAR_IREGISTRUOTI.csv.

        Скачать можно на сайте Registrų centras.

        Формат файла должен быть CSV с разделителем | (pipe).

⚙️ Установка и Сборка
Вариант 1: Использование Python (исходный код)
Bash

git clone https://github.com/your-username/rc-patcher.git
cd rc-patcher
python rc_patcher.py

Вариант 2: Сборка EXE файла (для серверов без Python)

Для использования на клиентских машинах или серверах 1С рекомендуется скомпилировать скрипт в один исполняемый файл:
Bash

pip install pyinstaller
pyinstaller --onefile rc_patcher.py

Файл rc_patcher.exe появится в папке dist.
💻 Использование

Утилита запускается из командной строки.
Синтаксис
Bash

rc_patcher.exe <ПУТЬ_К_ПАПКЕ> <ИМЯ_XML_ФАЙЛА> [РЕЖИМ_ПЕРЕЗАПИСИ]

Параметры

    ПУТЬ_К_ПАПКЕ: Полный путь к каталогу, где лежат файл платежки и файл JAR_IREGISTRUOTI.csv.

    ИМЯ_XML_ФАЙЛА: Название файла с выгрузкой в банк (например, export.xml).

    РЕЖИМ_ПЕРЕЗАПИСИ (необязательно):

        1 (или true, overwrite) — Скрипт перезапишет исходный файл.

        0 (по умолчанию) — Скрипт создаст новый файл с суффиксом _UPDATED.xml.

Примеры
Bash

# Создать новый файл plat_UPDATED.xml (безопасный режим)
rc_patcher.exe "C:\BankExport" "plat.xml" 0

# Перезаписать исходный файл plat.xml (режим для интеграции)
rc_patcher.exe "C:\BankExport" "plat.xml" 1

🔌 Интеграция с 1С:Предприятие

Для автоматической подмены имен при выгрузке платежек из 1С добавьте вызов скрипта в конце процедуры выгрузки (после ЗаписьXML.Закрыть()).

Пример кода на встроенном языке 1С:
Фрагмент кода

// ... (Код формирования XML завершен, файл закрыт)

// Настройки путей
КаталогФайла = "C:\Exchange\"; // Путь, где лежит XML и CSV
ИмяФайлаXML = "sepa_payment.xml";
ПутьКExe = КаталогФайла + "rc_patcher.exe";

// Проверка наличия скрипта
ФайлСкрипта = Новый Файл(ПутьКExe);
Если ФайлСкрипта.Существует() Тогда
    // Формируем команду: exe "папка" "файл" 1 (перезапись)
    // Важно: Следите за кавычками, если в путях есть пробелы
    Команда = """" + ПутьКExe + """ """ + КаталогФайла + """ """ + ИмяФайлаXML + """ 1";
    
    // Запуск с ожиданием завершения
    ЗапуститьПриложение(Команда, , Истина);
    
    Сообщить("Имена получателей обновлены по базе Центра регистров.");
Иначе
    Сообщить("Скрипт rc_patcher не найден, отправляем как есть.");
КонецЕсли;

🔍 Как это работает (Логика)

    Скрипт загружает файл JAR_IREGISTRUOTI.csv в оперативную память (Hash Map).

    Парсит XML файл, находя все транзакции (CdtTrfTxInf).

    Извлекает код получателя из тега <Id><OrgId><Othr><Id>.

    Ищет этот код в загруженной базе данных.

        Если найден: Сравнивает имя в XML с именем в базе. Если они отличаются, заменяет содержимое тега <Nm> на официальное название.

        Если не найден: (например, иностранная компания или физлицо) — оставляет запись без изменений.

    Сохраняет результат.

⚠️ Важные замечания

    Кодировка: Скрипт ожидает файлы в кодировке UTF-8.

    Иностранные компании: Если код получателя не найден в базе Литвы (например, код Болгарской компании), скрипт выведет в лог сообщение NOT FOUND и пропустит эту транзакцию. Это нормальное поведение.

    Формат CSV: Убедитесь, что файл Центра регистров использует разделитель | (вертикальная черта).

📄 Лицензия

MIT License. Свободное использование и модификация.